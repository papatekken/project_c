extends layout

block content
  .w3-container
    if locals.user
      h4 Just Test.
      img(src="/temp_funny.jpg")
    else
      h4 You are not logged in! Please #[a(href="/login") Log In] to continue.